import util.Token;
import util.ast.AbstractSyntaxTree;

import java.util.ArrayList;

public class Parser {
    public static AbstractSyntaxTree toAST(ArrayList<Token> tokens) {
        AbstractSyntaxTree ast = new AbstractSyntaxTree();
        return ast;
    }
}
